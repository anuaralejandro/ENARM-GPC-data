# Requirements for GPC Medical Classification
# PyTorch and transformers for medical NLP

# PyTorch with CUDA support (Windows GPU)
# Install separately: conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
# OR pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Transformers and NLP
transformers>=4.35.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
accelerate>=0.25.0  # Para optimización de GPU

# Data processing
pandas>=2.1.0
tqdm>=4.66.0

# Already in main requirements.txt:
# - numpy
